<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Profile Details</title>
  <link rel="icon" href="assets/favicon.png" />
  <style type="text/css" media="all">
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Futura Std Condensed Light', sans-serif;
    }

    /* Body Styling */
    body {
      background: rgb(59, 11, 12); /* Dark red background */
      background-image: url('../assets/background.jpg'); /* Background image */
      color: #fff; /* White text */
      height: 100vh;
      width: 100%;
      font-family: 'Futura', sans-serif;
    }

    /* Header Styling */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.5rem 3rem;
      position: relative;
     
    }

    .fimg {
      width: 143px;
      height: 143px;
    }

    nav {
      display: flex;
      justify-content: center;
      gap: 30px;
      margin: 0;
      font-size: 1.1rem;
      width: 40%;
    }

    .nav a {
      color: #fff;
      text-decoration: none;
      padding: 8px 15px;
      border-radius: 8px;
      opacity: 0.8;
      font-weight: 500;
      transition: opacity 0.3s ease;
    }

    .nav a:hover, .nav a.active {
        background-color:darkgray;
      color: white;
      opacity: 1;
      border-radius: 20px;
    }

    /* Main Content Area */
    .main {
      display: flex;
      justify-content: center;
      align-items: center;
      height: calc(100vh - 80px);
    }

    .container {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 100%;
    }

    .signup-box {
     
      padding: 2rem;
      border-radius: 12px;
      text-align: center;
      max-width: 600px;
      width: 100%;
      
    }

    h2 {
      margin-bottom: 1.5rem;
      font-size: 36px;
      font-weight: 700;
      color:darkgray;
    }

    .profile-details {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
      width: 100%;
    }

    .form-field {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem;

      border-radius: 8px;
      transition: all 0.3s ease;
    }

    .form-field:hover {
      background-color:gray;
     
    }

    label {
      font-size: 18px;
      font-weight: 600;
      color: darkgray;
      text-align: left;
      flex: 0.5;
    }

    span {
      font-size: 18px;
      color: #fff;
      font-weight: 500;
      flex: 1;
      text-align: left;
    }

    .days-left-value {
      font-weight: bold;
      color: darkgray;
      font-size: 20px;
    }

    /* Login Text Styling */
    .login-text {
      margin-top: 2rem;
      font-size: 22px;
    }

    .login-link {
      color: darkgray;
      font-weight: 600;
      text-decoration: none;
    }

    .login-link:hover {
      text-decoration: underline;
    }

    /* Responsive Styles */
    @media (max-width: 768px) {
      .signup-box {
        padding: 1.5rem;
      }

      .nav {
        flex-direction: column;
        gap: 15px;
      }
    }
  </style>
</head>
<body>
  <!-- Header Section -->
  <header class="header">
    <!-- Logo -->
    <a href="home">
      <img class="fimg" src="/assets/popcornlogo.png" alt="Popcornbox" class="logo" />
    </a>
    <!-- Navigation Links -->
    <nav class="nav">
      <a href="profiledetails">Profile Details</a>
      <% if (user.role === 'admin') { %> <!-- Check if the user is an admin -->
        <a href="admindashboard">Dashboard</a>
      <% } %> 
      <a href="edit-profile">Edit Profile</a>
    </nav>
    
  </header>

  <!-- Main Content -->
  <div class="main">
    <div class="container">
      <div class="signup-box">
        <!-- Title -->
        <h2>Profile Details</h2>
        <!-- Profile Information -->
        <div class="profile-details">
          <div class="form-field">
            <label>First Name:</label>
            <span><%= user.fname %></span>
          </div>
          <div class="form-field">
            <label>Last Name:</label>
            <span><%= user.lname %></span>
          </div>
          <div class="form-field">
            <label>Email:</label>
            <span><%= user.email %></span>
          </div>
          <div class="form-field">
            <label>Role:</label>
            <span><%= user.role %></span>
          </div>
          <div class="form-field">
            <label>Purchased Plan:</label>
            <span>
              <% if (subscriptionPlan.plan_name) { %>
                <%= subscriptionPlan.plan_name %>
              <% } else { %>
                No plan available
              <% } %>
            </span>
          </div>
          
          <div class="form-field">
            <label>Buy Date:</label>
            <span>
              <% if (purchasedplan.purchaseDate) { %>
                <%= new Date(purchasedplan.purchaseDate).toDateString() %>
              <% } else { %>
                No purchase date available
              <% } %>
            </span>
          </div>
          <div class="form-field">
            <label>Expiry Date:</label>
            <span>
              <% if (purchasedplan.expiryDate) { %>
                <%= new Date(purchasedplan.expiryDate).toDateString() %>
              <% } else { %>
                No expiry date available
              <% } %>
            </span>
          </div>
          
          <div class="form-field days-left">
            <label>Days Left:</label>
            <span class="days-left-value"><%= daysLeft %></span>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
