<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Profile Details</title>
  <link rel="icon" href="assets/favicon.png" />
 <style type="text/css" media="all">
   /* General Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Futura Std Condensed Light', sans-serif;
}

/* Body Styling */
body {
  background: rgb(59, 11, 12); /* Dark red background */
  background-image: url('../assets/background.jpg'); /* Background image */
  color: #fff; /* White text */
  height: 100vh;
  width: 100%;
}
.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 4rem;
    position: relative;
    
  }
  
  .logo {
    width: 50px;
    height: 50px;
  }
  

  
  .nav a {
    color: white;
    text-decoration: none;
    font-size: 1.1rem;
    opacity: 0.7;
    transition: opacity 0.3s;
    padding: 5px 15px;
  border-radius: 5px;
  }
  
  nav a:hover, nav a.active {
    background-color:darkgray;
    border-radius: 20px;
    color: white;
  
  }
  .fimg{
    width: 143px;
    height: 135px;
  }
  
  
  /* Navigation Menu */
  nav {
    display: flex;
    justify-content: center;
    gap: 30px;
    margin: 20px 0;
    font-size: 1.2rem;
    width: 60%;
  }
  .main{
    display: flex;
    justify-content: center;
    align-items: center;
  }
  .container {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
  }
  .signup-box {
    padding: 2rem;
    border-radius: 10px;
    text-align: center;
    max-width: 500px; /* Limit width for smaller screens */
  }
  
  h2 {
    margin-bottom: 1rem;
    font-size: 36px;
    font-weight: bold;
  }
  
  /* Profile Details Styling */
  .profile-details {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
  }
  
  /* Form Field Styling */
  .form-field {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.8rem;
    border-bottom: 1px solid #555;
  }
  
  label {
    font-size: 18px;
    font-weight: bold;
    text-align: left;
  }
  
  span {
    font-size: 18px;
    color: #ccc;
  }
  
  
  
  .days-left-value {
    font-weight: bold;
    color: #ff7f50;
    font-size: 20px;
  }
  
  /* Login Text Styling */
  .login-text {
    margin-top: 1rem;
    font-size: 24px;
  }
  
  .login-link {
    color: white;
    font-weight: bold;
    text-decoration: none;
  }
  
  .login-link:hover {
    text-decoration: underline; /* Underline link on hover */
  }
 </style>
</head>
<body>
  <!-- Header Section -->
  <header class="header">
    <!-- Logo -->
     <a href="home">
        <img
      class="fimg"
      src="/assets/popcornlogo.png"
      alt="Popcornbox"
      class="logo"
    />
     </a>
    
    <!-- Navigation Links -->
    <nav class="nav">
      <a href="profiledetails">Profile Details</a>
      <a href="edit-profile">Edit Profile</a>
    </nav>
  </header>

  <!-- Main Content -->
  <div class="main">
    <div class="container">
      <div class="signup-box">
        <!-- Title -->
        <h2>Profile Details</h2>
        <!-- Profile Information -->
        <div class="profile-details">
          <div class="form-field">
            <label>First Name:</label>
            <span><%= user.fname %></span>
          </div>
          <div class="form-field">
            <label>Last Name:</label>
            <span><%= user.lname %></span>
          </div>
          <div class="form-field">
            <label>Email:</label>
            <span><%= user.email %></span>
          </div>
          <div class="form-field">
            <label>Role:</label>
            <span><%= user.role %></span>
          </div>
          <div class="form-field">
            <label>Purchased Plan:</label>
            <span><%= subscriptionPlan.plan_name %></span>
          </div>
          <div class="form-field">
            <label>Buy Date:</label>
            <span><%= purchasedplan.purchaseDate %></span>
          </div>
          <div class="form-field">
            <label>Expiry Date:</label>
            <span><%= purchasedplan.expiryDate %></span>
          </div>
          <div class="form-field days-left">
            <label>Days Left:</label>
            <span class="days-left-value"><%= daysLeft %></span>
          </div>
        </div>

        
        </div>
      </div>
    </div>
  </div>
</body>
</html>