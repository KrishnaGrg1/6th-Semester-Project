<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify OTP</title>
  <link rel="stylesheet" href="css/login.css">
</head>
<body>
  <div class="container">
    <div class="login-box">
      <h2>Verify OTP and Set New Password</h2>
      <p class="subtitle">Please enter the OTP sent to your email and set a new password.</p>

      <!-- Display message if provided -->
      <% if (message) { %>
        <div id="message" style="color: red;"><%= message %></div>
      <% } %>

      <!-- Form for verifying OTP and setting new password -->
      <form action="/verify-otp" method="POST">
        <!-- Hidden input to pass email -->
        <input type="hidden" name="email" value="<%= email %>">
        
        <!-- OTP Input -->
        <label for="otp">Enter OTP</label>
        <input type="text" id="otp" name="otp" placeholder="Enter OTP" required>

        <!-- New Password Input -->
        <label for="newPassword">New Password</label>
        <div class="password-wrapper">
          <input type="password" id="password" name="password" placeholder="Enter new password" required>
          <span class="toggle-password" onclick="togglePassword('password', this)">👁️</span>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="login-btn">Set New Password</button>
      </form>

      <!-- Link to Login page -->
      <p class="signup-text">
        Remembered your password? <a href="/login" class="signup-link">Log In</a>
      </p>
    </div>
  </div>

  <script src="js/script.js"></script>
  
</body>
</html>
